Syntactic parsing, then, is the task of recognizing a sentence and assigning a syntactic structure to it.

This chapterpresents three of the most widely used parsing algorithms for automatically assigning a complete context-free (phrase-structure) tree to an input sentence.

a sentence that cannot be parsed may have grammatical errors (or at least be hard to read)